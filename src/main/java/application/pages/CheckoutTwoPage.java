package application.pages;

import application.utils.WebDriverManager;

public class CheckoutTwoPage extends BasePage {
    public CheckoutTwoPage(WebDriverManager webDriverManager) {
        super(webDriverManager);
    }
}
